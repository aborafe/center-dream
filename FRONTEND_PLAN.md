# سنتر دريم — خطة واجهة Laravel قابلة للربط

## نطاق هذه المرحلة

واجهة عربية RTL بتصميم أعمال حديث. صفحاتها Routes وControllers وBlade مستقلة، وجاهزة لاستبدال بيانات العرض مؤقتًا بـ Models وQueries عند بدء الباك إند؛ لا تعتمد على شاشة SPA واحدة أو روابط `#`.

## لقطة الحالة — 10 سبتمبر 2026

- [x] Routes مسمّاة للوحة الحسابات والطلاب والاشتراكات والمواد والتقارير والملف والإعدادات.
- [x] بحث عام بـ `GET /search` ونتائج ظاهرة في سجل الطلاب.
- [x] صفحة طباعة مستقلة `GET /receipts/print` بمقاس A4 أو A5.
- [x] تطبيع رقم واتساب المصري (`010...` إلى `2010...`) دون حذف أرقام إضافية.
- [x] ربط لوحة الحسابات والطلاب والمواد والمدرسين والمستخدمين والتقارير بـ Models وQueries حقيقية.
- [x] توحيد النظام البصري ومعاينة صفحة الدخول عند 320px و480px و768px و1024px و1440px دون تمرير أفقي.

## المسارات العاملة

| المنطقة | Route | View |
| --- | --- | --- |
| لوحة الحسابات | `/` | `screens/dashboard.blade.php` |
| سجل الطلاب | `/students` | `screens/students.blade.php` |
| تسجيل اشتراك | `/subscriptions/create` | `screens/subscription.blade.php` |
| تحصيل ووصل | `/collections` | `screens/collections.blade.php` |
| المواد | `/inventory` | `screens/inventory.blade.php` |
| الإدارة الأكاديمية | `/academics` | `screens/academics.blade.php` |
| المدرسون | `/teachers` | `screens/teachers.blade.php` |
| المستخدمون والصلاحيات | `/users` | `screens/users.blade.php` |
| التقارير | `/reports` | `screens/reports.blade.php` |
| الملف والإعدادات | `/profile`, `/settings` | صفحات Blade مستقلة |
| وصل الطباعة | `/receipts/print` | `receipts/print.blade.php` |
| بوابة المدرس | `/my-subjects` | `screens/teacher-portal.blade.php` |

## قواعد قبل بدء أي شاشة

- RTL أولًا، بلا تمرير أفقي بين 320px و1440px.
- لا رابط `#` لتدفق أعمال؛ لكل شاشة رابط Laravel واضح.
- Controllers رفيعة وForm Requests للتحقق، والاستعلامات الفعلية لاحقًا في Models/Scopes أو طبقة مخصصة.
- الحقول لها `name` وlabel و`autocomplete`، والأيقونات لها اسم وصول.
- لا تُعرض التفاعلات المحلية كأنها عمليات مالية محفوظة قبل الربط بقاعدة البيانات.

## الأدوار

| الدور | نطاق الوصول المستهدف |
| --- | --- |
| مسؤول المركز | كل الصفحات والحركات والصلاحيات |
| سكرتير | الطلاب، التسجيل، التحصيل، الوصل وواتساب؛ والباقي يظهر عند منح الصلاحية |
| مدرس | مواده وطلابه وحالة السداد فقط |

## شاشات الواجهة

1. دخول Laravel يجهز للتحقق الفعلي من الحساب.
2. لوحة ملخص مالي وإجراءات سريعة.
3. تسجيل متعدد المواد، خصم، تحصيل، وإجماليات حية.
4. الطلاب: بحث، ملف طالب، مواد مشتركة ومتاحة، مدفوعات، واتساب ووصل.
5. السنة الدراسية والصفوف والمواد والمدرسون.
6. المستخدمون ومصفوفة الصلاحيات.
7. الخصومات، مدفوعات المدرسين، والتقرير المالي.
8. بوابة المدرس: المواد والطلاب وحالة السداد للعرض فقط.

## تدفقات الأعمال المستهدفة

- التحصيل يربط الطالب والمادة والسكرتير وطريقة الدفع، ويعرض بيانات الطالب المسجل فور إدخال هاتفه.
- الخصم مبلغ أو نسبة مع سبب وصاحب الإجراء.
- صرف المدرس يربط المدرس والفترة والمبلغ.
- الوصل يفتح معاينة طباعة، ويمكن حفظه PDF من نافذة المتصفح.
- واتساب يفتح `wa.me` برسالة جاهزة ورقم الطالب.

## مراحل التنفيذ والمتابعة

### المرحلة 1 — التنقل والهيكل

- [x] فصل المسارات الأساسية وViews Blade.
- [x] تحويل البحث العام إلى GET مع التحقق.
- [x] فصل الوصل عن واجهة التطبيق للطباعة.
- [x] مراجعة أزرار الإجراءات في صفحات الواجهة الحالية لتقود إلى route أو action حقيقي.
- [x] إضافة تسجيل دخول فعلي وMiddleware للمصادقة والصلاحيات على المسارات.

**بوابة مراجعة:** `php artisan route:list` و`php artisan view:cache` يمران بلا أخطاء.

### المرحلة 2 — نظام التصميم

- [x] توحيد مقاسات المحتوى والبطاقات والمسافات في كل الشاشات.
- [x] تحديث مكوّنات `x-page-header` و`x-input` لتدعم href وname وautocomplete بصورة سليمة.
- [x] إعادة تصميم لوحة الحسابات والجداول والفلاتر وحالات عدم وجود بيانات.
- [x] مراجعة القائمة القابلة للطي والواجهة الصغيرة.

**بوابة مراجعة:** تدقيق `web-design-guidelines` واختبارات viewport الخمسة.

### المرحلة 3 — بيانات الأعمال والصلاحيات

- [x] migrations: users, roles, permissions, students, academic_years, grades, subjects, enrollments, payments, discounts, teacher_payouts.
- [x] Models والعلاقات والفهارس الأساسية وبيانات مرجعية أولية.
- [x] حفظ اشتراك مادة أو مواد متعددة وتحصل مستقل لكل مادة داخل معاملة واحدة في قاعدة البيانات.
- [x] تسجيل تحصيل فعلي بوصـل مستقل من رقم الدفعة، وواتساب بتطبيع الرقم المصري.
- [x] إظهار صلاحيات المدير والمدرس والسكرتير بموجب Middleware حقيقي؛ المدير كامل الصلاحيات، ويمكن منح السكرتير صلاحيات مباشرة لكل حساب.
- [x] نماذج الخصم والاشتراك متعدد المواد وصرف المدرسين تحفظ فعليًا.
- [x] اختبارات Feature للمصادقة وحماية الصلاحيات والطباعة والواتساب؛ تبقى اختبارات التدفقات المالية بعد الحفظ الفعلي.

**بوابة مراجعة:** قاعدة بيانات جديدة قابلة للتهيئة واختبارات الأعمال تمر بالكامل.

## معايير التنفيذ

- الصفوف والجداول تستجيب لـ 320 و480 و768 و1024 و1440 بكسل.
- تستخدم الأزرار الأيقونية `aria-label`، والحقول أسماء و`autocomplete`، وتوجد حالات تركيز مرئية.
- تخف الحركات عند `prefers-reduced-motion`.
- قبل الربط بقاعدة البيانات، تُعرض البيانات الحالية كطبقة واجهة مؤقتة فقط ولا تُعامل كعمليات محفوظة.

## المرحلة 4 — استكمال التشغيل ببيانات واقعية

**الحالة الحالية:** بدأ التنفيذ. المسارات الأساسية تعمل، لكن تدفق ملف الطالب ورد المبالغ وجرد المادة يحتاجون إكمالًا قبل اعتباره نظامًا ماليًا مكتملًا.

### 4.1 بيانات العرض والاختبار

- [x] إنشاء `DemoDataSeeder` مستقل لا يُشغّل تلقائيًا في بيئة الإنتاج.
- [x] تضمين مدير وسكرتير ومدرسين وطلاب ومواد واشتراكات: مكتملة وجزئية وملغاة.
- [x] تضمين دفعات وخصومات وصرف مدرس؛ لتغذية اللوحة والتقارير والمحافظ.
- [x] إضافة اختبار Feature يثبت إمكانية تشغيل الـSeeder دون أخطاء وأن العلاقات المالية الأساسية موجودة.

**بوابة مراجعة:** `php artisan migrate:fresh --seed` للبيانات المرجعية و`php artisan db:seed --class=DemoDataSeeder` للبيانات التجريبية، ثم فحص العدادات في قاعدة البيانات.

### 4.2 ملف الطالب وتدفقات التحصيل

- [x] إضافة مادة من نافذة داخل ملف الطالب مع مبلغ مدفوع وخصم اختياري.
- [x] فتح تحصيل الدفعة من ملف الطالب في نافذة مختصرة، مع نجاح/طباعة/واتساب بعد الحفظ.
- [x] عرض سجل الدفعات والخصومات والردود داخل ملف الطالب.
- [x] تحويل إلغاء الاشتراك ورد المبلغ إلى حركة مالية صريحة تظهر في التقرير، وليس قيمة وصفية فقط.
- [x] منع تكرار إرسال التحصيل نفسه باستخدام `submission_token` فريد.
- [x] تعديل بيانات الطالب، ملاحظاته، وحذف الطالب بتأكيد صريح.

**بوابة مراجعة:** اختبارات Feature لتسجيل مادة ودفعة وخصم وإلغاء/رد، مع فحص إجمالي الطالب والتقرير.

### 4.3 المدرسون والمواد

- [x] ربط عملية صرف المدرس بمادة واحدة أو توزيع واضح على مواد متعددة.
- [x] عرض سجل تحصيل المادة، عدد الدفعات، والطلاب الملغين في جرد المواد.
- [x] إظهار عدد الطلاب المشتركين من إجمالي طلاب الصف في بطاقة المادة.
- [x] ملف المدرس يعرض تحصيل موادِه وديون الطلاب والصرف والمحفظة المتاحة.

**بوابة مراجعة:** اختبار أرقام محفظة مدرس بمادة ذات دفعات كاملة وجزئية وصرف مسجل.

### 4.4 إدارة البيانات وتجربة الواجهة

- [x] تعديل/إيقاف المدرس والمادة والمستخدم من الواجهة، مع الحفاظ على السجل المالي.
- [x] توحيد أيقونات SVG في القائمة والبحث وإجراءات صف الطلاب.
- [x] مراجعة تفاعلية لملف الطالب عند 320 و480 و768 و1024 و1440 بكسل باستخدام بيانات الـSeeder دون تجاوز أفقي.

**بوابة مراجعة:** تشغيل `php artisan test --compact` و`npm run build`، مع توثيق نتائج اختبار العرض.

## سجل التحقق

- 10-09-2026: `vendor/bin/pint --format agent` نجح بعد إضافة Route الوصل وForm Request.
- 10-09-2026: `npm run build` نجح بعد فصل الطباعة إلى صفحة مستقلة.
- 10-09-2026: `php artisan view:cache` نجح بعد تحديث صفحات Blade.
- 10-09-2026: معاينة 320px و1440px للوحة الحسابات، و320px للطلاب، و1440px للاشتراك دون overflow مرئي.
- 10-09-2026: `php artisan test --compact tests\\Feature\\CenterPagesTest.php` نجح: 6 اختبارات، 41 تحققًا.
- 10-09-2026: أضيفت Routes مستقلة للإدارة الأكاديمية والمدرسين والمستخدمين؛ فحص Feature الحالي: 6 اختبارات، 41 تحققًا.
- 10-09-2026: شُغلت migrations وReferenceDataSeeder، وأضيف تسجيل الدخول الفعلي وأمر `php artisan center:create-admin` لإنشاء مدير بصورة آمنة.
- 10-09-2026: فُعلت صلاحيات المسارات واختُبرت: `CenterPagesTest` نجح بـ 7 اختبارات و47 تحققًا.
- 10-09-2026: أصبح حفظ الاشتراك فعليًا: البحث/الإنشاء بالهاتف، ثم `Enrollment` و`Payment` في DB transaction، مع منع تجاوز الرصيد. الاختبار الحالي: 8 اختبارات و48 تحققًا.
- 10-09-2026: رُبطت لوحة التحكم والسجل والجرد والمدرسون والمستخدمون والتقارير بالبيانات الفعلية؛ وأضيفت بوابة مدرس مقيدة بالمواد والطلاب المسندين لحسابه. الاختبارات: 9 اختبارات و50 تحققًا.
- 10-09-2026: مراجعة Playwright لصفحة الدخول عند 320px: لا يوجد تمرير أفقي، وحقول الهوية وكلمة المرور لها labels وautocomplete صحيح.
- 11-09-2026: أضيف حفظ الخصم الحقيقي عبر `ApplyEnrollmentDiscount` مع قفل الاشتراك داخل معاملة، وحُذف ملف الطالب الوهمي لصالح Route Model Binding. `pint` وFeature tests نجحت: 11 اختبارًا و59 تحققًا.
- 11-09-2026: Playwright فحص صفحة الدخول عند 320px و480px و768px و1024px و1440px؛ `scrollWidth === clientWidth` في المقاسات الخمسة. أُعيد بناء Vite بعد اكتشاف غياب manifest أثناء الفحص.
- 11-09-2026: أصبح التسجيل يدعم صفوف مواد متعددة، مع منع تكرار المادة وحساب حي للرسوم والمدفوع والمتبقي. حفظ الصفوف والمدفوعات يجري ذريًا في قاعدة البيانات، ويثبت اختبار منفصل أن فشل صف واحد يلغي كامل العملية. `pint` وFeature tests: 13 اختبارًا و68 تحققًا، وBlade cache وVite build نجحت.
- 11-09-2026: أضيف حفظ صرف المدرسين بالفترة والطريقة والمنفذ، وظهر إجمالي الصرف وصافي التحصيل في التقرير. راجع Playwright شاشة الصرف عند 320px دون تجاوز أفقي. `pint` وFeature tests: 14 اختبارًا و72 تحققًا.
- 11-09-2026: أضيفت فلاتر التقرير الفعلية للتاريخ والمنفذ والمدرس ونوع الحركة، وسجل تدقيق موحّد للتحصيل والصرف. راجع Playwright الفلاتر والسجل عند 320px دون تجاوز أفقي. `pint` وFeature tests: 15 اختبارًا و78 تحققًا.
- 11-09-2026: رُبطت إعدادات المركز بجدول `center_settings` وحفظ فعلي بدل الرسالة المؤقتة السابقة. طُبقت migration محليًا، وراجع Playwright نموذج الإعدادات عند 320px دون تجاوز أفقي. `pint` وFeature tests: 16 اختبارًا و82 تحققًا.
- 11-09-2026: أضيف إنشاء مستخدم فعلي وربط الدور والصلاحيات المباشرة عبر `permission_user`. راجع Playwright شبكة الصلاحيات عند 320px وتعطيلها تلقائيًا لدور المدير. `pint` وFeature tests: 17 اختبارًا و87 تحققًا.
- 11-09-2026: أضيف نموذج حفظ المادة التعليمية مع السنة والصف والمدرس والسعر، وبمنع تكرار المادة داخل السنة والصف. `pint` وFeature tests: 18 اختبارًا و90 تحققًا.
- 11-09-2026: أضيف نموذج حفظ المدرس برقم هاتف اختياري وحالة نشطة، ليصبح ربط المدرس بالمادة قابلًا للإدارة من الواجهة. `pint` وFeature tests: 19 اختبارًا و93 تحققًا.
- 11-09-2026: أضيف بحث الطالب في صفحة الاشتراك برقم الهاتف عبر Route JSON فعلي؛ يملأ الاسم ويعرض الصف والسنة والمواد الحالية مباشرة. راجع Playwright السلوك عند 320px دون تجاوز أفقي. `pint` وFeature tests: 20 اختبارًا و97 تحققًا.
- 11-09-2026: أصبح ملف الطالب يعرض المواد المتاحة لسنة وصف الطالب بعد استبعاد المواد المشترك بها. `pint` وFeature tests: 21 اختبارًا و100 تحقق.
- 11-09-2026: أضيف إنشاء السنة الدراسية والصف من واجهة الإدارة الأكاديمية، مع ضمان سنة نشطة واحدة. `pint` وFeature tests: 22 اختبارًا و107 تحققات.
- 11-09-2026: صححت عناصر جرد المواد التي كانت بلا فعل؛ صار «عرض الطلاب» يفلتر سجل الطلاب بالمادة عبر Route حقيقي. `pint` وFeature tests: 23 اختبارًا و110 تحققات.
- 11-09-2026: تدقيق نهائي: حُدثت اختبارات المتصفح القديمة لتغطي شاشة الدخول الحالية عند 320–1440px، وصُحح اختبار الضيف الافتراضي لسلوك التحويل إلى الدخول. المجموعة الكاملة نجحت: 30 اختبارًا و128 تحققًا، مع نجاح `pint` وBlade cache وVite build.
- 11-09-2026: عند إنشاء مستخدم بدور مدرس يُنشأ تلقائيًا ملف `Teacher` مرتبط به، فتعمل بوابة المدرس بمجرد إسناد المواد. المجموعة الكاملة: 31 اختبارًا و131 تحققًا، مع نجاح `pint` وBlade cache وVite build.
- 11-09-2026: أعيد تصميم تدفق الطالب حول ملف مالي موحد: رسوم/مدفوع/متبقٍ لكل مادة، تاريخ الاشتراك، تعديل بيانات الطالب، إلغاء الاشتراك مع مبلغ رد وملاحظة، ونافذة تأكيد التحصيل مع الطباعة وواتساب. أضيف token فريد للدفعة يمنع تكرار التحصيل عند إعادة إرسال الطلب، وملف مدرس يوضح محافظ المواد والتحصيل والديون والصرف. المجموعة الكاملة: 32 اختبارًا و135 تحققًا، مع نجاح `pint` وBlade cache وVite build.
- 11-09-2026: أضيف `DemoDataSeeder` آمن من التشغيل في الإنتاج، وفيه 5 طلاب و6 اشتراكات و6 دفعات وخصم وصرف وإلغاء لاختبار السيناريوهات الفعلية. اختباره المستقل نجح بـ 7 تحققات؛ وشُغّل أيضًا على قاعدة المشروع دون حذف السجلات السابقة.
- 11-09-2026: أضيف تعديل/إيقاف المدرسين والمواد والمستخدمين عبر نماذج داخل الصفحة، دون حذف السجل المالي. اختبارات Feature الكاملة نجحت: 36 اختبارًا و161 تحققًا.
- 11-09-2026: اختبار Playwright تفاعلي ببيانات الـSeeder: تحصيل 100 ج.م لسارة محمد من ملفها، وظهور نافذة النجاح مع رابط الوصل A5 ورابط WhatsApp مهيأ؛ واعتماد خصم لنور أحمد مع عودة مباشرة لملفه؛ وإلغاء اشتراكه ورد 20 ج.م، ثم ظهرت الحركة في سجل ردود الطالب وتقرير `type=refund`. فُحص ملف الطالب عند 320 و480 و768 و1024 و1440 بكسل دون تجاوز أفقي مرئي.
- 11-09-2026: فحص Playwright بصري للصفحات المعدلة (المدرسون، الإدارة الأكاديمية، المستخدمون) عند 320 و1440 بكسل؛ لم يظهر تجاوز أفقي، وأزرار التعديل تظل إجراءات مستقلة عن روابط الملفات.
